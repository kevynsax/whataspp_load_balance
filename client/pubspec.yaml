name: whatsapp_load_balance
description: An client for whats app load balance

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  intl: ^0.15.7
  provider: ^2.0.0+1
  shrine_images: ^1.0.0
  http: ^0.12.0+2

dev_dependencies:
  pedantic: ^1.4.0
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
